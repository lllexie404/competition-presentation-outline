---
name: "competition-presentation-outline"
description: "将产品/系统方案文档整理为面向竞赛评审的汇报大纲。生成结构化大纲（HTML/MD/PPTX 三格式）+ 评审维度×章节×落点映射表 + 时间分配与超时降级预案 + 每页'论点+支撑+讲解稿'三件套（含业务化'一句话纲领'）+ 评审追问支撑附录（Q-x）+ 证据强度标注，并支持 DEMO 演出脚本与桥接话术。Invoke when 用户请求生成竞赛/评审汇报大纲、12分钟汇报、PPT+DEMO 汇报大纲、评审维度汇报、方案路演汇报。"
---

# 竞赛汇报大纲生成器 Competition Presentation Outline

将「从产品/系统方案文档 → 生成面向评审的竞赛汇报大纲」这一高复用流程封装为可复用 Skill。核心目标：让产出的大纲**对齐评审维度、节奏可控、话术分众、追问有备**。

## 何时使用

- 用户有一份产品/系统方案文档，需要面向竞赛评审产出结构化汇报大纲。
- 用户要求生成企业技能竞赛、产品评审、方案路演的汇报大纲。
- 用户提到「12 分钟汇报」「PPT + 系统 DEMO」「评审维度汇报」等场景。

## 六步主流程

### Step 1 · 需求澄清与骨架选型

收集关键信息，决定叙事骨架与语言口径：

| 澄清项 | 说明 | 影响 |
| --- | --- | --- |
| 竞赛/汇报类型 | 财经技能/产品设计/技术架构等 | 决定评审维度模板库 |
| 时长 | 硬约束（如 12′） | 驱动时间分配算法 |
| 形式 | PPT / PPT+DEMO / 纯演讲 | 决定是否嵌入 DEMO 脚本 |
| 受众画像 | 业务/技术/混合、甲方/乙方 | 决定语言口径与深度分配 |
| 评审维度列表 | 名称 + 权重（可选） | 决定叙事骨架与落点映射 |
| 行业赛道 | 金融/能源/制造/互联网 | 决定术语库与类比库 |

**叙事骨架库**：The Case / The Report-Out / 三幕式（痛点-方案-价值）/ 问题-方案-价值。根据受众与评审维度选择并融合定制。

### Step 2 · 内容映射与时间分配

1. **文档通读与核心信息抽取**：架构、机制、规则、场景、交付物、指标。
2. **维度 × 章节 × 落点映射表**：把评审维度逐一分配到具体章节，标注论证方式。
3. **时间分配 + 优先级分级**：硬约束下按「必演/可压缩/可跳」三级分级。
4. **三幕章法编排**：每幕设桥接句，做因果衔接。

时间校验：各章节分钟数之和必须等于总时长。

### Step 3 · 细节填充与话术打磨

1. 每页「**论点 + 支撑点 + 讲解稿**」三位一体。
2. 「**一句话纲领**」：讲解开头用业务化语言先给结论，再展开。
3. **三层冗余控制**：正讲抛概念 / DEMO 做实证 / 附录做追问支撑，互不重复。
4. **业务化语言翻译**：技术术语 → 业务人员能听懂的类比。
5. **口径统一**：隐去假设性数值、统一验收口径、统一术语命名。

### Step 4 · 交叉复核（六层逻辑模型）

| 层 | 核验内容 |
| --- | --- |
| 结构逻辑 | 叙事弧与桥接承接是否自洽 |
| 内容逻辑 | 因果链是否闭合（痛点→方案→价值） |
| 交叉一致性 | 大纲 vs 原文档逐字段比对、内部前后一致 |
| 时间逻辑 | 全程时长算术、子项加总、超时预案 |
| 证据逻辑 | supported / stated-only / inferred 三级标注 |
| 受众逻辑 | 语言口径与评委画像匹配 |

### Step 5 · 评审追问预判

- 识别高概率被追问点：**处理顺序、量化依据、成熟度、使用场景、关键规则逻辑**。
- 生成 Q-x 备查答案，含「业务化话术」与「技术化话术」两套口径。

典型处理：对「为什么是这个处理顺序」这类问题，用「盖楼先打地基」/「做饭先备菜」类比，并主动在叙事桥接处圆场，消除追问口子。

### Step 6 · 多格式产出

同一份结构化数据渲染为多种格式：
- **HTML 报告版**：带时间分配图 + 六维度覆盖雷达图，响应式深色金融主题，自包含。
- **Markdown 手动版**：纯文本便于人工修改。
- **PPTX 演示版**：可直接演示的幻灯片。
- **DEMO 衔接脚本**：PPT 与系统 DEMO 的切换话术与超时降级预案。

## 输入 Schema

| 字段 | 类型 | 必填 | 说明 |
| --- | :-: | :-: | --- |
| `document` | file | 是 | 原始产品/方案文档（MD/Word/PDF） |
| `duration_minutes` | number | 是 | 汇报总时长（分钟） |
| `presentation_format` | enum | 是 | `ppt` / `ppt_demo` / `speech` |
| `audience` | enum | 是 | `business` / `technical` / `mixed` |
| `positioning` | enum | 否 | `client_internal` / `vendor_delivery` / `default` |
| `evaluation_dimensions` | array | 是 | 评审维度列表（名称+权重可选） |
| `industry` | enum | 否 | 金融/能源/制造/互联网等 |
| `output_formats` | array | 否 | `html` / `md` / `pptx` / `all` |

## 输出 Schema

| 产出物 | 说明 |
| --- | --- |
| 结构化大纲 | HTML / MD / PPTX 三格式 |
| 评审维度 × 章节 × 落点映射表 | 对齐评分点 |
| 时间分配图 + 六维度覆盖雷达图 | 可视化节奏与覆盖 |
| 每页「论点+支撑+讲解稿」 | 含「一句话纲领」 |
| 评审追问支撑附录（Q-x） | 含业务/技术双话术 |
| 超时降级预案 | 必演/可压缩/可跳分级 |
| 证据强度标注 | supported / stated-only / inferred |

## 模板库资产

- **叙事骨架库**：The Case / The Report-Out / 三幕式 / 问题-方案-价值。
- **行业类比话术库**：金融/能源/制造…
- **常见评审质疑模板库**：处理顺序 / 量化依据 / 成熟度 / 落地性 / 场景…
- **三幕桥接句生成器**。
- **「一句话纲领」提炼规则**：先结论后展开 + 业务化类比。

## 边界声明

- 本 Skill 解决「从文档到大纲的结构化编排与话术化包装」。
- **不替代**对方案本身深度的判断、创新点的战略拔高。
- 生成后需人工复核内容准确性。

## 输出目录

- 至少一个项目目录，如 `/workspace/<项目名>/`。
- HTML / MD / PPTX 与 source 文档置于同一项目目录。
- 向用户提供 `computer://` 链接。

## 参考

- 完整方法论来源：本 Skill 由「其他应收款穿透式风控管理系统 12 分钟竞赛汇报大纲」实战过程归纳提炼。
- 示例应用：六维度映射表、八段叙事、10 处「一句话纲领」、4 个 Q-x 追问支撑、超时降级预案。关键机制：双轨模型（主轨定量/辅轨定性）、「AI 只辅助、人做决策、全程留痕」。